# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_20", "bun"]

[phases.install]
cmds = [
  "bun install",
  "bunx prisma generate"
]

[phases.build]
cmds = []

[start]
cmd = "bun run src/index.ts"
